image: node:18

definitions:
  services:
    chrome:
      image: browserless/chrome

pipelines:
  default:
    - step:
        name: Install, Test & SonarCloud
        caches:
          - node
        services:
          - chrome
        script:
          # 1. Install Node dependencies
          - npm install

          # 2. Install Chrome for Karma Headless
          - apt-get update && apt-get install -y wget gnupg unzip
          - wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          - apt-get install -y ./google-chrome-stable_current_amd64.deb || apt-get install -f -y

          # 3. Run Angular Unit Tests with custom ChromeHeadlessNoSandbox
          - npm run test -- --watch=false

          # 4. Build Angular for production
          - npm run build -- --configuration production

          # 5. Run SonarCloud scan via Node script
          - node sonar.js
